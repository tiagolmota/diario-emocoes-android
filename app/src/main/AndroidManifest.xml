<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.isla.diarioemocoes">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiarioEmocoes"
        android:networkSecurityConfig="@xml/network_security_config"
        android:fullBackupContent="false">

        <!--
            SplashActivity: ponto de entrada — verifica consentimento RGPD
            e redireciona para ConsentimentoActivity ou MainActivity.
        -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.DiarioEmocoes.Splash"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity: actividade principal da app -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!--
            ConsentimentoActivity: ecrã RGPD obrigatório no primeiro arranque.
            Não pode ser iniciada externamente (exported="false").
        -->
        <activity
            android:name=".rgpd.ConsentimentoActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.DiarioEmocoes" />

        <!--
            PrivacidadeActivity: painel de direitos RGPD (Art. 15.º, 17.º, 7.º §3).
            Acessível via menu da ActionBar.
        -->
        <activity
            android:name=".rgpd.PrivacidadeActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:label="@string/privacidade_titulo" />

    </application>

</manifest>
